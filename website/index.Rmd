---
title: "Abstract"
knit: (function(input_file, encoding) {
  out_dir <- '../docs';
  rmarkdown::render(input_file,
 encoding=encoding,
 output_file=file.path(dirname(input_file), out_dir, 'index.html'))})
output:
  html_document:
    toc: true
    theme: darkly
---

```{r setup, include=FALSE}
here::i_am("website/index.Rmd")
```

```{r load data, include=FALSE}
# variables
todays_date <- readRDS(here::here("output",
                                  "todays_date.rds"))
```

The Coronavirus is an infectious disease that can have terminal effects.^[**World Health Organization**  _Coronavirus disease (COVID-19)_. [link](https://www.who.int/health-topics/coronavirus#tab=tab_1). 2022.] 



This project looks at ... and uses visuals such as tables, graphs, and maps to shows trends as of `r todays_date`. 







